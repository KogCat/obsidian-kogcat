# vscode
.vscode 

# Intellij
*.iml
.idea

# npm
node_modules

# Don't include the compiled files in the repo.
# They should be uploaded to GitHub releases instead.
main.js
meta.json

# Exclude sourcemaps
*.map

# obsidian
data.json

# Local build-time secrets (injected by esbuild, never committed)
secrets.local.json

# Exclude macOS Finder (System Explorer) View States
.DS_Store

# Claude Code
CLAUDE.md

# Internal docs (LLM-spec docs are not for public distribution)
docs/
